import { connect } from "react-redux";
import AutocompleteDropdown from "./autocomplete_dropdown";

const mstp = (state, ownProps) => {
  return {

  };
};

export default connect(mstp, null)(AutocompleteDropdown)